# ML 마스터 데이터 (PV only) — 컬럼 사전

> 파일: `ML_마스터데이터_PV.csv` · 3,066행(10/12~10/18, 1분 간격) · 결측 0
> 원칙: **PV값만** 수록(SP 전량 배제). **폐기 컬럼 제외**(`ML_weight`·`TT-1006_rate`·τ 관련 일체).
> 출처 조인: `labels_recycle1.csv` + 원본 `Attachment1.csv`(PV) + `DP_data.xlsx`(DP_reactor), Timestamp 기준.
> 표기: [주어짐] 실측/스펙 · [계산] 파생·역산.

## 키 / 분할

| 컬럼 | 설명 |
|---|---|
| `Timestamp` | 1분 간격 시각 |
| `Campaign` | 10/12 · 10/13 · 10/14 · 10/15 · 10/16 · 10/17-18 |
| `Split` | 정상운전검사(10/12 앵커) · 학습(10/13–16) · 검증(10/17–18 held-out) |

## 입력 (모델에 실제로 넣는 변수)

| 컬럼 | 정체 | 쓰임 |
|---|---|---|
| `TT-1006.PV` | 반응기 입구온도(°C) [주어짐] | ONNX1·ONNX2 공유 입력 |
| `FRC-1004.PV` | 벤젠/프로펜 질량유량비(무차원) [주어짐] | ONNX1·ONNX2 공유 입력 |
| `FT-1004.PV` | 프로펜 스트림 유량(kg/h) [주어짐] | **ONNX1(전환율) 입력** + 라벨 계산 재료 |
| `FT-1003.PV` | 벤젠 스트림 유량(재순환 합류 후, kg/h) [주어짐] | 총유량 계산용 |
| `total_flow_kgh` | **총 질량유량 = FT-1003 + FT-1004** [계산] (10/12=18000) | **ONNX2(ΔP) 입력** |

## 라벨 (출력 — 실측 아님)

| 컬럼 | 정체 | 쓰임 |
|---|---|---|
| `X_conv_r1_deployed` | 프로펜 전환율, 재순환 큐멘 **1 mol%(r1)** 가정. SRK 상태방정식 **구간이분법**으로 AT-1100(출구밀도) 역산 [계산] | **배포 ONNX1 타깃** (검증 재현 RMSE 0.020987) |
| `X_conv_r0` | 같은 방식, 재순환 0%(r0) 변형 [계산] | 재순환 민감도 비교용(비배포). RMSE 0.022220 |
| `DP_reactor_kPa` | 반응기 압력강하 = **PT-1004 − 25 − PT-1100** [계산]. 25=HX-1005 튜브측 0.25 bar [주어짐, App B] | **배포 ONNX2 타깃** (10/12=29.22) |

> ⚠ 컬럼 이름 주의: 원본 `labels_recycle1.csv`의 `X_propene_conversion`은 실제로 **r0**, `X_propene_recycle1`이 **배포 r1**이다. 여기선 혼동 없게 `X_conv_r1_deployed`/`X_conv_r0`로 재명명했다.

## 라벨 재료 (모델 입력 금지 — 넣으면 누출)

| 컬럼 | 정체 | 주의 |
|---|---|---|
| `PT-1004.PV` | 프로펜 공급압(kPa) [주어짐] | ΔP 라벨 재료. **입력 금지(누출)** |
| `PT-1100.PV` | 반응기 출구압(kPa) [주어짐] | ΔP 라벨 재료 + SRK P. **입력 금지(누출)** |
| `AT-1100.PV` | 반응기 출구밀도(kg/m³) [주어짐] | 전환율 역산의 SRK 목표값 |
| `TT-1100.PV` | 반응기 출구온도(°C) [주어짐] | SRK T + 에너지수지 |

## 재순환 (라벨 가정 재료)

| 컬럼 | 정체 |
|---|---|
| `FT-1300.PV` | 벤젠 재순환 유량(kg/h) [주어짐] |
| `FT-1001.PV` | 신선 벤젠 유량(kg/h) [주어짐] |

## 독립 검증 전용

| 컬럼 | 정체 | 쓰임 |
|---|---|---|
| `FT-1201.PV` | 큐멘 제품 유량(kg/h) [주어짐] | 큐멘 물질수지 대조(하류 체류시간 유의) |
| `FT-1002.PV` | 벤젠 유량 실측(kg/h) [주어짐] | FRC×FT-1004 역산 검산 |
| `dT_adiabatic_C` | 단열 온도상승 = TT-1100 − TT-1006 [계산] | 에너지수지 독립 검증 |
