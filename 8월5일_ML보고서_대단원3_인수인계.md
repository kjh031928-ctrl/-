# 8월 5일 — ML 보고서 대단원 3 진행 인수인계

> **이 파일 하나로 새 채팅에서 그대로 이어갈 수 있게 정리한 문서다.**
> 선행: `8월4일_저녁_ONNX2배선_시뮬검증_보고서준비.md`(§10 데이터 용어), `2026-07-21_ML개발/CLAUDE.md`(절대규칙)
> 산출물: `ML보고서_초안_대단원3.md`(초안 v0.1)

---

## 📋 새 채팅 첫 메시지에 붙여넣을 프롬프트

```
ML 보고서 대단원 3(ML) 초안을 이어서 쓰려고 해. 먼저 이 파일들을 읽고 상황 파악해줘:
1. 8월5일_ML보고서_대단원3_인수인계.md  ← 현재 상황·결정·목차(이 파일)
2. ML보고서_초안_대단원3.md  ← 작성 중인 초안
3. 8월4일_저녁_ONNX2배선_시뮬검증_보고서준비.md §10  ← 데이터 용어 통일
4. 2026-07-21_ML개발/CLAUDE.md  ← 절대규칙

규칙: 임의 판단 금지, 근거(ref) 없이 동작 금지, 지시 없이 파일 생성 금지, 토큰 낭비 금지.
숫자는 데이터로 재계산해 검증. [주어짐]/[계산]/[가정] 구분. 질문할 땐 내 질문을 함께 적어 답해줘.
```

---

## 0. 한 줄 요약
대단원 3(ML) **목차 확정 + 초안 v0.1 작성 완료.** 남은 건 claude code 수치 재현·그림 2종·모델 문헌 인용. 최종은 규칙대로 .docx 렌더링.

---

## 1. 분담 (조원 ↔ 나)
- **대단원 2 (조원):** 입출력 변수 세트의 *선정 근거*(최소화 원칙·영향변수 비교·경계 논거). 출력 **후보군**을 준다:
  - set1: 입력 [TT-1006, FT-1004, FRC-1004] / 출력 후보 [밀도, 전환율]
  - set2: 입력 [TT-1006, 총 질량유량(FT-1003+FT-1004), FRC-1004] / 출력 후보 [DP, PT-1100]
- **대단원 3 (나·ML):** 후보를 *받아서* 최종 출력 선택 → 모델 계열 선정(스크리닝) → 학습(develop) → 평가·검증. **통합·케이스는 이후 보고서.**

---

## 2. 확정 목차 (대단원 3)

- **3.1 학습 문제 정의**
  - 3.1.1 지도학습·회귀 (라벨 존재 → 지도학습, 비지도 해당 없음)
  - 3.1.2 후보 중 최종 출력 선정 — **전환율 X**(밀도 제외), **ΔP**(PT-1100 제외)
- **3.2 데이터 사용 방식**
  - 3.2.1 보통최소제곱(OLS, 무가중)
  - 3.2.2 캠페인(날짜) 분할 — 학습 13–16 / held-out 17–18 / 10/12 앵커, 무작위 금지
  - 3.2.3 가중최소제곱(WLS) 고려와 기각 — 투명성 서술만
- **3.3 후보 모델군과 특징** [ref필요]
- **3.4 스크리닝 설계** — 정확도(RMSE)·물리 27점·외삽·재현환경
- **3.5 스크리닝 결과와 모델 선정**
  - 3.5.1 전체 후보 요약 (22개) [claude code 원본 필요]
  - 3.5.2 전환율(ONNX1) 심층 비교 · 3.5.3 압력강하(ONNX2) 심층 비교 · 3.5.4 선정 결론(선형)
- **3.6 모델 상세 사양·학습 구성** — 3.6.1 ONNX1(로짓 이유 포함) · 3.6.2 ONNX2 · 3.6.3 정밀도/재현
- **3.7 정확도 평가와 검증** — 지표 정의 · 물리 27점 · 독립검증(에너지·물질수지) · (선택)과도
- **3.8 한계와 정직한 맞바꿈**

→ 대단원 3은 **평가·검증에서 종료.**

---

## 3. 이 대화에서 확정한 결정·서술 (그대로 유지)

**출력 선정**
- set1 → **전환율**(밀도 제외): 전환율은 반응 진행도의 근본 자유도, 밀도는 조성·T·P 파생 종속량(직접 예측 시 보존식 위반 위험).
- set2 → **ΔP**(PT-1100 제외): `PT-1100 = P1 − ΔP`, P1은 플로우시트 계산 → ΔP만 알면 PT-1100 자동 결정(종속량). ΔP는 절대압 ~1.1% 작은 미지량이라 견고.
- **"계측기 있어서 불필요"는 근거 아님.** 모델 목적은 계측기 없는 시뮬에서 값 공급. 제외 근거는 "종속량"이다.

**입력에서 압력 제외 (Q&A 방어)**
- 유량과 거의 공선(그림자 변수)이라 유량이 압력 정보 대부분 흡수 — *부분적으로만* 맞는 근거.
- 반드시 함께 쓸 것: **케이스 스터디에서 압력 지정 불가.** 임의값을 넣으면 ① 설정 유량과 물리적 모순, ② 정직한 값은 플로우시트가 ΔP(ML)로 계산 → 순환 의존.

**용어 정리 (혼동 해소)**
- **OLS(보통최소제곱) = 무가중.** 오차 제곱합 최소화 + 모든 점 동일 가중. WLS는 Σwᵢrᵢ² 최소화.
- **held-out = 학습에서 완전히 빼두고 최종 성능 측정에만 쓰는 데이터** (우리 10/17–18).
- **로짓 선형은 별도 모델 아님.** "1차 선형회귀가 좋은데 전환율이 [0,1] 벗어나서 로짓 공간에서 적합" → develop 단계(3.6.1)에서 서술. ΔP는 제약 없어 무변환.
- **물리 타당성** = 예측이 물리 방향과 맞는지(온도↑·유량↑ 방향 27점). **외삽** = 학습 범위 밖(365°C) 거동(트리는 clamp). **재현환경** = sklearn/numpy 버전·seed=42.
- **스크리닝 vs 평가·검증:** 스크리닝=여러 후보 상대 비교로 고르기 / 평가·검증=고른 하나를 물리·독립근거로 절대 검증(에너지·물질수지). 배포본은 13–18 재학습본이라 평가는 최종본 확인.

**무가중(OLS) 채택 근거 (투명성)**
- WLS 시험 → 무가중 대비 ΔRMSE ≈ 0.000132(무의미). WLS는 노이즈바닥·3σ·창길이·퍼지 등 정당화 어려운 손잡이 다수(조원도 한계 9개 명시).
- "안 해봐서가 아니라 따져보고 뺐다." CLAUDE.md 규칙: WLS 수식·τ·3σ를 *근거로* 인용 금지, 기각 서술로만.

**데이터 용어** → handoff §10 표 참조(운전 실측 / 라벨 역산값 / 분할 / Appendix B 스펙 / 시뮬 계산값 / 케이스 조건).

---

## 4. 남은 재료 (초안 완성 전)
1. **[claude code] §3.5.1 22개 모델 결과표** — 일괄 비교 원본 확보.
2. **[claude code] 수치 재현** — ΔRMSE 0.000132, ONNX1 RMSE 0.020987·R² 0.98290·큐멘 3.05%, ONNX2 RMSE 0.1910·배포계수, 27점 격자, 에너지수지 상관, 큐멘 물질수지, 자기상관 0.997.
3. **[그림] §3.5.3** — 총유량 vs 프로펜유량 **물리 방향성 그래프** + 최종 F2 **parity plot**. (데이터: `DP_data.xlsx` + CSV의 FT-1003 + 배포계수, 준비됨)
4. **[ref] §3.3** — 선형·GBR(Friedman 2001)·RF(Breiman 2001)·물리회귀 문헌 확인.
5. **[확인] ΔP 라벨의 HX-1005 손실 25 kPa** — 측정값인지 가정치인지. (DP_data.xlsx 수식 `=PT1004−25−PT1100`)

---

## 5. 핵심 파일
- 초안: `ML보고서_초안_대단원3.md`
- 데이터 용어: `8월4일_저녁_ONNX2배선_시뮬검증_보고서준비.md` §10
- 규칙: `2026-07-21_ML개발/CLAUDE.md`, 루트 `CLAUDE.md`
- 데이터: 원본 `2026 Chemical..._Attachment1.csv`, `DP_data.xlsx`, `2026-07-21_ML개발/data/processed/cumene_ml_training_data.xlsx`, WLS 가중치 `전체데이터_전환율_가중치.csv`(업로드)
- ONNX: `2026-07-21_ML개발/onnx/reactor_conversion_r1.onnx`(ONNX1), `.../onnx/reactor_dp.onnx`(ONNX2)
