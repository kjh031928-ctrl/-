"""run_factorial.py — 전체 조합 실행.

담당: 공용 / 구현 예정 Phase: 5.

역할: 24개 조합(모델2 x 입력세트3 x 재순환2 x 가중치2)을 scikit-learn 기본값으로
실행하고 results/{config_hash}.json 에 설정+지표+실행시각 저장(지시서 §4.5).
학습=Split '학습', 검증=Split '검증'. 무작위 분할 금지(절대규칙 0.4).

[STUB] Phase 1 구조 생성 단계. 아직 구현 없음.
"""
