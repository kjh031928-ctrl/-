# SRK 순성분 상수 변경 근거 및 전환율 재계산 기록

**문서 목적** ML 학습 타깃(프로펜 전환율)을 산출하는 SRK 상태방정식의 순성분 상수를 문헌값에서 Process Simulation 내장값으로 교체한 경위, 검증 절차, 재계산 방법 및 영향을 기록한다.

**대상 파일** `ML_마스터데이터_PV.csv` (컬럼 `X_conv_r0`, `X_conv_r1_deployed`)
**원본 보존** `ML_마스터데이터_PV_원본(Sheet1상수).csv`
**참조 파일** `SRK constant.xlsx` (Sheet1 = 문헌값, Sheet2 = 시뮬레이터 내장값 캡처)

---

## 1. 배경

본 과제의 ML 학습 타깃인 프로펜 전환율 $X$ 는 직접 계측되지 않는다. 반응기 출구에 설치된 밀도계(AT-1100)와 온도계(TT-1100), 압력계(PT-1100)의 실측값으로부터 SRK 상태방정식을 이용해 역산한다.

$$\rho = \frac{\bar M}{V_m}, \qquad \bar M = \frac{\dot m}{n_{tot}}, \qquad n_{tot} = n_0 - n_{P0}X$$

알킬화 반응이 2 mol → 1 mol 이므로 전환율이 커질수록 총 몰수가 줄고 평균분자량이 커진다. SRK는 주어진 $T$, $P$, 조성에서 몰부피 $V_m$ 을 공급하는 역할을 하며, 실측 밀도와 계산 밀도가 일치하는 $X$ 를 반복 계산으로 찾는다.

따라서 **SRK 순성분 상수($T_c$, $P_c$, $\omega$)와 이원 상호작용 계수($k_{ij}$)는 학습 타깃 자체를 결정하는 입력**이다.

## 2. 문제 인식

전환율 산출에 사용한 상수는 문헌값(`SRK constant.xlsx` Sheet1)이었다. 그러나 최종 공정 모델은 Process Simulation의 SRK 물성 패키지(`SRKSearchOrder = System:SIMSCI`)로 구축되어 있으며, 시뮬레이터 내장 라이브러리의 순성분 상수는 문헌값과 미세하게 다르다.

이 상태에서는 다음 불일치가 발생한다.

- ML 학습 타깃은 **문헌값 기반 SRK**로 산출된 전환율
- 그 ML 모델이 삽입되어 동작하는 공정 모델은 **시뮬레이터 내장 SRK**로 물성을 계산

동일한 열역학 모델(SRK)을 쓰면서 상수만 다른 두 계산이 하나의 통합 모델 안에 공존하게 되므로, **학습–배포 간 열역학 기준을 일치시키기 위해** 시뮬레이터 내장값(Sheet2)으로 통일하였다.

## 3. 두 상수 집합의 차이

### 3.1 순성분 상수

| 성분 | 항목 | Sheet1 (문헌값) | Sheet2 (시뮬레이터 내장) | 차이 |
|---|---|---|---|---|
| Benzene | $T_c$ (K) | 562.60 | 562.16 | −0.44 |
| | $P_c$ (kPa) | 4924.39 | 4898 | −26.4 (−0.54 %) |
| | $\omega$ | 0.209 | 0.210024 | +0.49 % |
| Propene | $T_c$ (K) | 364.95 | 365.57 | +0.62 |
| | $P_c$ (kPa) | 4620.42 | 4665 | +44.6 (+0.97 %) |
| | $\omega$ | 0.1435 | 0.139817 | −2.57 % |
| Propane | $T_c$ (K) | 369.82 | 369.83 | +0.01 |
| | $P_c$ (kPa) | 4249.57 | 4248 | −1.6 |
| | $\omega$ | 0.1529 | 0.152291 | −0.40 % |
| Cumene | $T_c$ (K) | 631.05 | 631.10 | +0.05 |
| | $P_c$ (kPa) | 3208.96 | 3209 | +0.04 |
| | $\omega$ | 0.3266 | 0.325809 | −0.24 % |

가장 큰 차이는 **프로펜의 편심인자(−2.6 %)와 임계압력(+1.0 %)** 이다.

### 3.2 분자량

시뮬레이터 내장값(78.1136 / 42.0806 / 44.0965 / 120.194)은 기존 사용값과 유효숫자 범위에서 동일하여 영향이 없다.

### 3.3 이원 상호작용 계수

시뮬레이터 내장 $k_{ij}$ 는 문헌값과 **완전히 일치**하므로 변경하지 않았다.

| 성분쌍 | $k_{ija}$ | $k_{ijb}$ | $k_{ijc}$ | $k_{ijd}$ | 적용 온도범위 |
|---|---|---|---|---|---|
| Benzene–Propane | 0.02 | 0 | 0 | 0 | 310 – 477 K |
| Propene–Propane | 0.0083 | 0 | 0 | 0 | 200 – 500 K |
| 나머지 4쌍 | 0 | 0 | 0 | 0 | — |

온도 의존항($k_{ijb}$ ~ $k_{ijd}$)이 모두 0이므로 상수 $k_{ij}$ 로 처리하였다.

## 4. 기존 값이 Sheet1로 계산되었음을 확인한 절차

수정 전, 기존 컬럼이 실제로 문헌값 기반인지 독립 구현으로 검증하였다.

**방법** Sheet1 상수와 위 $k_{ij}$ 로 SRK 혼합물 몰부피를 계산하고, 실측 밀도와 일치하는 $X$ 를 Brent 법으로 역산하여 `X_conv_r0` 와 대조.

**결과**

| 시점 | 독립 재현값 | `X_conv_r0` | 오차 |
|---|---|---|---|
| 10-12 15:00 | 0.615779 | 0.615885 | −1.1 × 10⁻⁴ |
| 10-13 16:59 | 0.502989 | 0.503103 | −1.1 × 10⁻⁴ |
| 10-15 20:57 | 0.744590 | 0.744677 | −0.9 × 10⁻⁴ |
| 10-16 20:26 | 0.310433 | 0.310547 | −1.1 × 10⁻⁴ |
| 10-18 00:55 | 0.927987 | 0.928083 | −1.0 × 10⁻⁴ |

전 구간 최대 오차 $1.2 \times 10^{-4}$, 부호 일정. 이 정도의 계통편차는 기체상수 자릿수(8.314 vs 8.314462) 또는 수렴 허용오차 차이로 설명되며, **계산 경로가 동일함을 의미한다.**

대조군으로 Sheet2 상수를 넣으면 오차가 $7.6 \times 10^{-4}$ 로 6배 커진다. 즉 기존 값은 Sheet1로 계산된 것이 맞다.

## 5. 재계산 방법

두 전환율 컬럼의 성격이 다르다.

| 컬럼 | 정의 |
|---|---|
| `X_conv_r0` | 공급 벤젠 스트림을 순벤젠으로 가정한 SRK 역산값 |
| `X_conv_r1_deployed` | 리사이클로 유입되는 큐멘을 공급 조성에 반영한 값 (ONNX 학습에 실제 사용) |

`X_conv_r1_deployed` 는 공급 벤젠 스트림의 큐멘 질량분율을 다음과 같이 보정한 것으로 확인되었다.

$$w_{\text{cumene}} = 0.015108 \times \frac{\text{FT-1300}}{\text{FT-1002}}$$

계수 0.015108은 리사이클 벤젠이 큐멘 약 0.99 mol% 를 함유할 때의 질량분율이며, 컬럼 오버헤드 규격(큐멘 ≤ 1 mol%)과 정합한다. 정상상태 구간에서 이 가정으로 `X_conv_r1_deployed` 가 재현된다.

### 5.1 델타 방식 채택

전 구간을 처음부터 재계산하지 않고, **상태방정식 상수 교체의 순수 효과만 분리하여 원래 값에 더하는 방식**을 사용하였다.

$$X_{\text{new}} = X_{\text{old}} + \Big[\, X_{\text{SRK}}(\text{Sheet2}) - X_{\text{SRK}}(\text{Sheet1}) \,\Big]$$

각 행의 실측값($T$, $P$, $\rho$, 유량)과 공급 조성 가정을 완전히 고정한 상태에서 EOS 상수만 바꿔 두 번 역산하고 그 차이를 취한다.

**채택 이유**

1. 재현 과정에서 발생하는 계통편차($1.2 \times 10^{-4}$)가 뺄셈으로 상쇄되어, 기존 값의 다른 특성이 보존된다.
2. `X_conv_r1_deployed` 의 램프 구간 처리 세부(평활 또는 지연 보정 여부)를 몰라도 상수 교체 효과를 정확히 반영할 수 있다.
3. 상수 교체가 만든 변화만 순수하게 분리되므로 변경 이력이 명확하다.

### 5.2 방법의 타당성 확인

$\Delta X$ 가 공급 큐멘 분율에 민감하지 않음을 확인하였다.

| 시점 | $\Delta X$ ($w_c = 0$) | $\Delta X$ ($w_c = 0.0097$) | 차이 |
|---|---|---|---|
| 10-12 15:00 | −0.0003913 | −0.0003935 | 2.2 × 10⁻⁶ |
| 10-15 20:57 | −0.0002400 | −0.0002426 | 2.6 × 10⁻⁶ |
| 10-18 00:55 | −0.0001581 | −0.0001613 | 3.2 × 10⁻⁶ |

민감도가 $\Delta X$ 자체의 0.6 % 수준이므로, 두 컬럼에 각각의 공급 조성 가정을 적용해도 결과는 사실상 동일하다.

## 6. 재계산 결과

전체 3,066행에 적용하였다.

| 항목 | `X_conv_r0` | `X_conv_r1_deployed` |
|---|---|---|
| 평균 (기존 → 수정) | 0.620294 → 0.619915 | 0.610585 → 0.610204 |
| 최소 (기존 → 수정) | 0.309950 → 0.309300 | 0.294032 → 0.293381 |
| 최대 (기존 → 수정) | 0.950851 → 0.950701 | 0.973067 → 0.972914 |
| 평균 변화 | −0.000379 | −0.000381 |
| 변화 범위 | −0.000652 ~ −0.000150 | −0.000652 ~ −0.000153 |

전 구간에서 **음의 방향으로 균일하게** 이동하며, 전환율이 높을수록 변화폭이 작다(0.93 부근 −0.00016, 0.31 부근 −0.00065).

**무결성 점검** 3,066행 × 20열 유지, 열 순서 동일, 전환율 외 18개 열 값 변화 없음, $0 < X < 1$ 만족, UTF-8 BOM 유지.

## 7. 영향 평가

변화량 0.038 %p 를 관련 오차원과 비교하면 다음과 같다.

| 항목 | 크기 |
|---|---|
| **SRK 상수 교체 (본 변경)** | **0.038 %p** |
| `X_conv_r1_deployed` 와 `X_conv_r0` 의 차이(리사이클 큐멘 보정) | 0.97 %p |
| ML 모델 홀드아웃 예측오차 | 약 1.1 %p |
| SRK 밀도경로와 단열 에너지수지 경로의 괴리 | 최대 7 %p |

전환율이 0.00038 이동하면 로지스틱 회귀의 로짓은 약 0.0016 이동하며, 이는 절편(−53.51) 대비 0.003 % 이다. **따라서 재학습 시에도 모델 계수는 유효숫자 범위에서 변하지 않는다.**

본 변경의 의의는 예측 정확도 개선이 아니라 **학습 타깃과 배포 환경의 열역학 기준을 일치시키는 데** 있다.

## 8. 미해결 사항

`X_conv_r1_deployed` 의 램프 구간(설정치 변경 중) 재현이 완전하지 않다. 정상상태에서는 위 5장의 리사이클 큐멘 보정으로 소수점 4자리까지 일치하나, 램프 구간에서는 최대 0.18 의 편차가 발생한다.

원인은 FT-1300 / FT-1001 의 과도상태 거동이 해당 시점의 반응기 실제 공급 조성과 일치하지 않기 때문으로 보인다(벤젠 공급탱크 T-1001 의 홀드업에 의한 지연). 원 계산에서 평활 또는 지연 보정을 적용했을 가능성이 있으며, 해당 절차가 확인되면 델타 방식이 아닌 전 구간 재계산으로 전환할 수 있다.

**단, 이 미해결 사항은 본 변경의 정확성에는 영향을 주지 않는다.** 델타 방식은 원 계산 절차와 무관하게 상수 교체 효과만 분리하기 때문이다.

## 9. 재현 절차

```
입력   ML_마스터데이터_PV_원본(Sheet1상수).csv
       TT-1100.PV [°C], PT-1100.PV [kPa], AT-1100.PV [kg/m3]
       FT-1003.PV, FT-1004.PV, total_flow_kgh, FT-1300.PV, FT-1002.PV

1. 공급 몰유량 산출
     프로펜 스트림 몰조성 프로펜 0.95 / 프로판 0.05
     r0용  공급 벤젠 = 순벤젠
     r1용  공급 벤젠 큐멘 질량분율 = 0.015108 x FT-1300/FT-1002

2. 각 행에 대해 Sheet1 상수로 X 역산
     SRK 혼합규칙  (a·alpha)mix = SUM_i SUM_j y_i y_j sqrt((a·alpha)_i (a·alpha)_j)(1 - k_ij)
                   b_mix = SUM_i y_i b_i
     P = RT/(V-b) - a·alpha/(V(V+b))  를 V 에 대해 Brent 법으로 풀이
     rho_calc = M_bar / V  가 실측 rho 와 같아지는 X 를 Brent 법으로 탐색

3. 동일 절차를 Sheet2 상수로 반복

4. delta_X = X(Sheet2) - X(Sheet1)
   X_new = X_old + delta_X

기체상수 R = 8.314 kJ/(kmol·K)
```

## 10. 보고서 기재 문안(안)

> 학습 타깃인 프로펜 전환율은 반응기 출구 밀도계(AT-1100)와 온도·압력 계측값으로부터 SRK 상태방정식을 이용해 역산하였다. 이때 순성분 상수와 이원 상호작용 계수는 문헌값이 아니라 **본 과제에서 사용한 Process Simulation의 내장 물성 라이브러리 값**을 적용하였다. ML 모델이 최종적으로 삽입되어 동작하는 환경과 학습 타깃 산출에 사용한 열역학 기준을 일치시키기 위함이며, 이를 통해 학습–배포 간 열역학 불일치를 제거하였다. 두 상수 집합 간 전환율 차이는 평균 0.038 %p 로 모델 자체의 예측오차(약 1.1 %p)보다 한 자릿수 작으나, 통합 모델의 내적 일관성 확보를 위해 적용하였다.
